import styled from '@emotion/styled';

export const Wrapper = styled.div`
  display: flex;
  flex-direction: column;
  padding: 1.5rem 3rem;
  width: 100%;
`;

export const SupervisionList = styled.div`
  display: flex;
  flex-direction: column;
  gap: 40px;
  height: 75vh;
  overflow-y: auto;
`;